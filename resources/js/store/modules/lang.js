
// States
const state = {
    lang: [],
};

// Getters
const getters = {};

// Mutations
const mutations = {
    changeLang (lang) {
        state.lang = lang;
    }
};

// Actions
const actions = {};

export default {
    state,
    getters,
    mutations,
    actions
}